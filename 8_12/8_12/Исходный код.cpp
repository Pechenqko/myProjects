#include <iostream>
#include <cmath>
#include <cstdlib>
int main(){
	int y = pow(8, 0.5);
	std::cout << (1 / 4) *sqrt((16 - 3*pow(y,4)) / pow(y,2)) *y + (sqrt((16 - 3 *pow(y,4)) / pow(y,2))* y *pow((tan(4 / sqrt(3*pow(y,4) - 16)))) / (sqrt(3 *pow(y,4) - 16),(-1));
	system("pause");
	return 0;
}

